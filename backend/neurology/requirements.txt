# IMPORTANT: Install PyTorch with CUDA support first!
# PyTorch 2.6+ is REQUIRED due to security vulnerability CVE-2025-32434
# For CUDA 12.1/13.0: pip install torch>=2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CUDA 11.8: pip install torch>=2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Then install: pip install -r requirements.txt

fastapi
uvicorn
transformers>=4.40.0
sentence-transformers>=2.5.0
# torch>=2.6.0  # Install separately with CUDA support (see above)
faiss-cpu
rank-bm25
nltk
pydantic
requests
accelerate>=0.26.0
peft>=0.8.0
bitsandbytes>=0.41.0
safetensors>=0.4.0